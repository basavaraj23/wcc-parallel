image:
  repository:

external_credentials:
  flare:
    auth_password:
  iron_source:
    secret:
  steam:
    api_key:

publishing_platform:
  user_api:

auto_scaling:
  min_replicas:
  max_replicas:
  target_cpu:
  proactive_replicas:

service_account:
  name: "apocalypse"

cleanup_redis:
  dry_run:
  campaign_cutoff:
  season_cutoff:

probes:
  liveness:
    enabled: true
    path: /
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 1
    successThreshold: 1
    failureThreshold: 15
  readiness:
    enabled: true
    path: /
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 1
    successThreshold: 1
    failureThreshold: 3


